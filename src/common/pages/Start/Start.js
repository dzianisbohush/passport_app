import React from 'react';

import GoogleBtn from '../../blocks/OauthButtons/GoogleBtn';
import Login from '../../blocks/OauthButtons/Login';
import Logout from '../../blocks/OauthButtons/Logout';

// eslint-disable-next-line no-unused-vars
const Start = props => (
  <div>
    <GoogleBtn />
    <Login />
    <Logout />
  </div>
);

export default Start;
